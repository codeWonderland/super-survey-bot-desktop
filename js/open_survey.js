// grab a link to the survey
let survey_link = document.getElementById("sbMainNavSectionListItemAnswer");

// if the element exists
if (survey_link) {
    // we click the link inside :3
    survey_link.getElementsByTagName("a")[0].click();
}